"use client";

import { AdminDegerlendirmelerView } from "../degerlendirmeler/page";

/** Kameramanlar dahil herkesin tüm kameramanların tüm raporlarını görebildiği sayfa. */
export default function TumDegerlendirmelerPage() {
  return <AdminDegerlendirmelerView baslik="Tüm Değerlendirmeler" />;
}
